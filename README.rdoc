== README

The app(https://beautifullybroken.herokuapp.com/) version is, as the name implies, broken.  I couldn't set up the SendGrid account properly, so I removed all the secrets, so while the page appears and generally works, it won't send any emails.

